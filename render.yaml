services:
  - type: web
    name: news-pulse-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NODE_VERSION
        value: 20
